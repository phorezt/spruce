{
  "name": "spruce",
  "version": "1.0.0",
  "description": "A service that is named spruce but might be a fir",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nikek",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
